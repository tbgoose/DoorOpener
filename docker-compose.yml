version: '3'

services:
  dooropener:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    volumes:
      - ./config.ini:/app/config.ini
    restart: unless-stopped
    networks:
      - dooropener_network

networks:
  dooropener_network:
    driver: bridge
